<h2>Maynard's Years Through Books</h2>

<% @years.each do |year| %>
  <h3><%= link_to year.year, year_path(year) %></h3>
  <% if year.books.any? %>
  <ol>
    <% year.books.each do |book| %>
      <li><%= link_to book.title, book_path(book) %></li>
    <% end %>
  </ol>
  <% end %>
<% end %>
